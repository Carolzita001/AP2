#include <stdio.h>
#include <math.h>
#include <locale.h>

int main (){
    setlocale (LC_ALL, "Portuguese_Brazil");

    int valor_N, i;

    printf("Insira um valor inteiro positivo: \n");
    scanf("%d", &valor_N);

    for (int valor_N;  < count; i++)
    {
        /* code */
    }
    
}